# PitchIQ — 실행에 필요한 핵심 의존성
# 생성 방법: venv(python 3.13)의 site-packages dist-info 기준으로 고정
# 재현 순서: python3.13 -m venv venv && source venv/bin/activate && pip install -r requirements.txt
#
# 참고) opencv-python 과 opencv-python-headless 가 동시에 설치되어 있으면
# cv2 임포트 충돌/DLL 문제의 원인이 될 수 있습니다. 서버/배포 환경에서는
# GUI가 필요 없으므로 opencv-python-headless 하나만 남기는 것을 권장합니다.
# (Day 1 트러블슈팅 블로그 참고)

# --- 데이터 수집 / 처리 ---
pandas==3.0.3
numpy==2.5.0
pybaseball==2.2.7
joblib==1.5.3

# --- 머신러닝 / 딥러닝 ---
scikit-learn==1.9.0
tensorflow==2.21.0
keras==3.15.0

# --- 컴퓨터 비전 / 객체 탐지 ---
opencv-python-headless==4.10.0.84
ultralytics==8.4.78
torch==2.12.1
torchvision==0.27.1
pytesseract==0.3.13   # OCR — 시스템에 tesseract 바이너리 별도 설치 필요 (brew install tesseract)
Pillow==12.2.0

# --- 시각화 ---
matplotlib==3.11.0
seaborn==0.13.2
plotly==6.8.0

# --- 웹 서비스 ---
streamlit==1.58.0

# --- 영상 다운로드 ---
yt-dlp==2026.6.9

# --- 기타 ---
requests==2.34.2
python-dateutil==2.9.0.post0
